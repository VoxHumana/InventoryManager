<Window x:Class="InventoryManager.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vw="clr-namespace:InventoryManager.Views"
             xmlns:cal="http://www.caliburnproject.org"
             MinHeight="200" MinWidth="750" >
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*" />
            <ColumnDefinition Width="50*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*" />
        </Grid.RowDefinitions>
        <vw:InventoryView cal:Bind.Model="{Binding NewInventoryModel}" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <vw:ProductListView cal:Bind.Model="{Binding ProductListModel}" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top"/>
    </Grid>
</Window>